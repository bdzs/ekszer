
CODE SEGMENT

ASSUME CS:CODE,DS:CODE

INT67 PROC NEAR


	JMP SHORT NEXT
DATA    DB "2345678901234567890"
NEXT    DB 00EAH
KILEP	DW 00
        DW 00

INT67 ENDP

MAIN PROC FAR

	PUSH DS
	XOR AX,AX
	MOV DS,AX
	MOV SI,4*67H
	MOV CX,02
	CLD
	PUSH CS
	POP ES
	MOV DI,OFFSET KILEP
	REPZ MOVSW
	POP DS

	MOV AX,2567H
	PUSH CS
	POP DS
	MOV DX,OFFSET INT67
	INT 21H

	MOV AX,3100H
	MOV DX,18H
	INT 21H

MAIN ENDP

CODE ENDS

END MAIN

